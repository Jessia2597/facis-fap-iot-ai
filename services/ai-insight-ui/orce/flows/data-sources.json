[
  {
    "id": "tab_aiui_data_sources",
    "type": "tab",
    "label": "AI Insight UI — Data Sources",
    "disabled": false,
    "info": "Pure UIBUILDER data-sources subflow per FAP §10. Receives FAP §9 commands from the dispatcher's link-to-data-sources and routes by msg._req.action. Synthesises a list of FACIS data sources from the simulation engine's last-known feed state plus static rows for non-simulation channels (SFTP). Returns msg._result for fn_build_response to wrap."
  },
  {
    "id": "link-from-ui-data-sources",
    "type": "link in",
    "z": "tab_aiui_data_sources",
    "name": "← from UI (data-sources)",
    "links": ["link-to-data-sources"],
    "x": 140,
    "y": 100,
    "wires": [["fn_data_sources_build"]]
  },
  {
    "id": "fn_data_sources_build",
    "type": "function",
    "z": "tab_aiui_data_sources",
    "name": "build data-sources result",
    "func": "const action = msg._req && msg._req.action;\nif (action !== 'data-sources.list') {\n    msg._result = { ok: false, errors: { action: 'data-sources: unknown action: ' + action } };\n    return msg;\n}\nconst FEED_KEYS = [\n  { key: 'latest_meters',    type: 'meter',         topic: 'facis.iot.meters.v1',         protocol: 'kafka', name: 'Smart Meters' },\n  { key: 'latest_pv',        type: 'pv',            topic: 'facis.iot.pv.v1',             protocol: 'kafka', name: 'PV Systems' },\n  { key: 'latest_weather',   type: 'weather',       topic: 'facis.iot.weather.v1',        protocol: 'kafka', name: 'Weather Stations' },\n  { key: 'latest_price',     type: 'price',         topic: 'facis.iot.prices.v1',         protocol: 'kafka', name: 'Energy Price Feed' },\n  { key: 'latest_consumers', type: 'load',          topic: 'facis.iot.loads.v1',          protocol: 'kafka', name: 'Consumer Devices' }\n];\nconst now = Date.now();\nconst sources = [];\nfor (const f of FEED_KEYS) {\n  const arr = global.get(f.key) || [];\n  const list = Array.isArray(arr) ? arr : Object.values(arr);\n  let lastTs = null;\n  for (const item of list) {\n    const ts = item && (item.timestamp || item.ts || item.observed_at || null);\n    if (ts && (!lastTs || new Date(ts).getTime() > new Date(lastTs).getTime())) lastTs = ts;\n  }\n  const ageMs = lastTs ? (now - new Date(lastTs).getTime()) : null;\n  let status = 'unknown';\n  if (lastTs) {\n    if (ageMs <= 5 * 60_000) status = 'healthy';\n    else if (ageMs <= 30 * 60_000) status = 'degraded';\n    else status = 'stale';\n  } else if (list.length === 0) {\n    status = 'inactive';\n  }\n  sources.push({\n    id: f.key, name: f.name, type: f.type, protocol: f.protocol, topic: f.topic,\n    entity_count: list.length, last_event_ts: lastTs,\n    last_event_age_seconds: ageMs ? Math.round(ageMs / 1000) : null,\n    status\n  });\n}\nconst engine = global.get('engine') || {};\nsources.push({\n  id: 'sftp_dsp_drop', name: 'DSP SFTP Drop', type: 'transfer', protocol: 'sftp', topic: null,\n  entity_count: (global.get('transfers') || []).length || 0,\n  last_event_ts: engine.simulation_time || null,\n  last_event_age_seconds: null, status: 'configured'\n});\nmsg._result = { ok: true, data: { sources, count: sources.length, engine_state: engine.state || 'unknown' } };\nreturn msg;\n",
    "outputs": 1,
    "x": 460,
    "y": 100,
    "wires": [["link-response-data-sources"]]
  },
  {
    "id": "link-response-data-sources",
    "type": "link out",
    "z": "tab_aiui_data_sources",
    "name": "→ Response (data-sources)",
    "mode": "link",
    "links": ["link-from-response"],
    "x": 760,
    "y": 100,
    "wires": []
  }
]
